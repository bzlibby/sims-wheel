{
	"version": "2.0.0",
	"tasks": [
		{
      "label": "Run tests",
      "type": "shell",
      "command": "yarn c8 --reporter=text --reporter=html mocha --forbid-only --recursive --sort \"test/**/*.js\"",
      "group": "test",
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "options": {"cwd": "${workspaceFolder}"}
		}
	]
}